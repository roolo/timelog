<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Timelog</title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <!-- Le styles -->
    <%= stylesheet_link_tag 'application', :media => 'all' %>
    <style>
      body {
        padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
      }
    </style>

    <!-- Le fav and touch icons -->
    <link href="<%= asset_path 'favicon.png' %>" rel="shortcut icon">
    <link href="<%= asset_path 'apple-touch-icon.png' %>" rel="apple-touch-icon">
    <link href="<%= asset_path 'apple-touch-icon-72x72.png' %>" rel="apple-touch-icon" sizes="72x72">
    <link href="<%= asset_path 'apple-touch-icon-114x114.png' %>" rel="apple-touch-icon" sizes="114x114">
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
          <div class="container-fluid">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="/">Timelog</a>
          <div class="container-fluid nav-collapse">
            <ul class="nav">
                <!--<li><%= link_to "Link1", "/path1"  %></li>-->
            </ul>
            <% if user_signed_in? %>
            <ul class="nav pull-right">
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                  <i class="icon-user icon-white"></i> <%= current_user %> <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li><a href="<%= edit_user_registration_path current_user %>">Edit account</a></li>
                  <li class="divider"></li>
                  <li><%= link_to 'Logout', destroy_user_session_path, method: :delete %></li>
                </ul>
              </li>
            </ul>
            <% end %>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container-fluid">
      <div class="row-fluid">

        <div class="span12">
          <% if user_signed_in? %>
            <%= form_for(@new_log) do |f| %>
              <%= f.text_field :title, placeholder: 'Type something to log and then hit enter...' %>
              <div class="row">
                <div class="span6">
                  <%= f.fields_for :topics, @new_topic do |sub| %>
                    <div class="row">
                      <div class="span3">
                      <%= sub.text_field :title, placeholder: 'Main topic' %>
                      </div>
                      <div class="span2">
                        <%= label_tag nil, nil, class: 'checkbox' do %>
                          <%= check_box_tag :lock_topic, nil, true %>&nbsp;Lock topic
                        <% end %>
                      </div>
                    </div>

                  <% end %>
                </div>
                <div class="span4">
                  <% ApplicationController::FLASH_BOOTSTRAP.each do |flash_level, bootstrap_level| %>
                    <% next if flash[flash_level].blank? %>
                    <div data-alert="alert" class="alert alert-<%= bootstrap_level %> fade in">
                      <a class="close" data-dismiss="alert" href="#">&times;</a>
                      <%= content_tag :p, flash[flash_level] %>
                    </div>
                  <% end # /flashes %>
                </div>
              </div>
            <% end # /form %>
          <% end %>
          <%= yield %>
        </div>

      </div><!--/row-->
      <hr/>
      <footer>
        <p>&copy; Rooland 2012</p>
      </footer>

    </div> <!-- /container -->

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
